# Base URL of portfolio-site-v6-prodigy-api. Used for real login.
PUBLIC_API_BASE_URL=http://localhost:8080

# Mock password still used by resetPassword()/verifyPassword() in src/stores/auth.ts -
# no real endpoint for those yet. Falls back to "admin" if unset. login() itself is real.
PUBLIC_ADMIN_PASSWORD=admin
