# Mock-auth credentials (see src/stores/auth.ts). Falls back to admin/admin
# if unset. Replace with a real API call once BlogController/auth exists.
PUBLIC_ADMIN_USERNAME=admin
PUBLIC_ADMIN_PASSWORD=admin
