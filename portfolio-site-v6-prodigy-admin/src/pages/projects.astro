---
import Layout from '../layouts/Layout.astro';
import NavbarComponent from '../components/NavbarComponent.vue';
import PlaceholderPage from '../components/PlaceholderPage.vue';
---

<Layout title="Admin Projects">
	<NavbarComponent client:load currentPath={Astro.url.pathname} pageTitle="Projects" />
	<PlaceholderPage
		client:load
		title="Projects"
		description="Project management is coming once the API is ready."
	/>
</Layout>
