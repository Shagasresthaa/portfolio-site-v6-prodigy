---
import Layout from '../layouts/Layout.astro';
import NavbarComponent from '../components/NavbarComponent.vue';
import PlaceholderPage from '../components/PlaceholderPage.vue';
---

<Layout title="Admin Blog">
	<NavbarComponent client:load currentPath={Astro.url.pathname} pageTitle="Blog" />
	<PlaceholderPage
		client:load
		title="Blog"
		description="Blog post management is coming once the API is ready."
	/>
</Layout>
